from django.urls import path

urlpatterns = [
    # Example endpoints for your Flutter app
    # path('tailors/', TailorListView.as_view(), name='tailor-list'),
    # path('catalogue/', CatalogueListView.as_view(), name='catalogue-list'),
]